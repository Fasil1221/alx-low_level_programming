#include "main.h"
/**
*_abs - prints the absolute value of a number
*@num: the number up for test
*Return: positive number
*/
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{

	return (-num);
	}
}
