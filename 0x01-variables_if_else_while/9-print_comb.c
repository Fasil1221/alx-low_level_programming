#include <stdio.h>
/**
 * main - combputchar(n);ination of single digits
 * Return: always 0
 */
	int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	if (n != 57)
{
	putchar(',');
	putchar(' ');
										}
	putchar('\n');

	return (0);
}
