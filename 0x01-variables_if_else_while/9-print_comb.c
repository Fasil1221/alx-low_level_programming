#include <stdio.h>
/**
 * main - combputchar(n);ination of single digits
 * Return: always 0
 */
	int main(void)
{
	int n;

	for (n = 47; n < 58; n++)

	if (n != 57)
{
	putchar(n);
	putchar(',');
	putchar(' ');
}									}
	putchar('\n');
	return (0);
}

