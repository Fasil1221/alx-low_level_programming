#include <stdio.h>
/**
 * main - lower case followed by new line
 * Return: always 0
 */
int main(void)
{
	char la;

	for (la = 'a'; ch <= 'z'; ch++)
	{
	if (la != 'e' && la != 'q')
	putchar(la);
	}

	putchar('\n');

	return (0);
}










